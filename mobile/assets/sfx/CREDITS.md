# Sound credits

- `hiss.wav`, `click.wav` — cut from **"Can of compressed air"** by *stilgar*, Wikimedia Commons, public domain
  (<https://commons.wikimedia.org/wiki/File:Can_of_compressed_air.ogg>). Source in `src/`, processing in `scripts/gen_sfx.py`.
- `rattle.wav`, `empty_rattle.wav`, `pool.wav` — synthesised by `scripts/gen_sfx.py`.
